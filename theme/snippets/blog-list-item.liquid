{% if style == "double" %}
    {% assign article_css_class = "post post-double" %}
{% else %}
    {% assign article_css_class = "post post-single" %}
{% endif %}

<div class="{{ article_css_class }}">
    <div class="post-inner">
        <div class="post-media{% if article.image_url == nil %} no-image{% endif %}" style="background-image: url('{{ article.image_url }}');">
            {% if article.category %}
                <div class="category-name">{{ article.category }}</div>
            {% endif %}
            <h3><a href="{{ article.url | absolute_url }}">{{ article.title }}</a></h3>
        </div>
        {% if article.excerpt %}
            <div class="post-descr">{{ article.excerpt }}</div>
        {% endif %}
        <div class="post-other">
            <div class="post-logo"><img src="{{ 'logo-mini.png' | asset_url }}" /></div>
            <div class="post-author">
                <div class="author">{{ article.author }}</div>
                <div class="date">{{ article.created_at | date: "%b %d, %Y" }}</div>
            </div>
        </div>
    </div>
</div>