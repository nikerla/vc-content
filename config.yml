defaults:
  -
   scope:
     path: ""
     type: "posts"
   values:
     layout: "my-site"
  -
   scope:
     path: "pages"
     type: "pages"
   values:
     layout: "project" # overrides previous default layout
     author: "No one"
